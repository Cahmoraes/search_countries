export default interface IThemeProps {
  title: string
  colors: {
    primary: string,
    secondary: string,
    background: string,
    shadow: string,
    text: string
    fill: string
    search: string
  }
}